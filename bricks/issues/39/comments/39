我检查了仓库当前结构，建议把这个 issue 从“直接切 Vercel 同域名部署”改成“先验证后落地”，原因如下：\n\n## 我确认到的仓库现状\n\n1. 当前仓库根目录没有 `vercel.json`，也没有根级 `package.json`。\n2. 前端当前已有 GitHub Pages 部署链路：`.github/workflows/deploy_web.yaml` 会在 push 到 `main` 后执行 `flutter build web --base-href "/${{ github.event.repository.name }}/" --release`，并发布 `apps/mobile_chat_app/build/web`。\n3. 后端当前是独立 Node/Express 服务，入口在 `apps/node_backend/src/index.ts`。\n4. `apps/node_backend/src/index.ts` 当前会执行数据库连接、自动 migration，并通过 `app.listen(PORT)` 启动传统服务进程；它不是按当前 issue 提议那样，天然可由根级 `routes` 直接映射成统一 API 入口。\n5. `apps/node_backend/README.md` 虽然写了 Vercel (Serverless) 示例，但仓库里真正存在的是 `apps/node_backend/vercel.json`，说明后端目前更接近“子项目独立部署”，而不是已经准备好做 monorepo 根级统一接入。\n\n## 所以我建议的调整\n\n### 不建议直接按当前 issue 的配置上线\n当前 issue 里的这段：\n\n```json\n{\n  "version": 2,\n  "routes": [\n    { "src": "/api/(.*)", "dest": "/apps/node_backend/src/index.ts" },\n    { "src": "/(.*)", "dest": "/apps/mobile_chat_app/web/index.html" }\n  ]\n}\n```\n\n风险很高，主要是：\n\n- `apps/node_backend/src/index.ts` 现在是传统 Express 启动模式，不是已经验证过的统一入口函数模式\n- `apps/mobile_chat_app/web/index.html` 是源码目录，不是 Flutter build 产物目录\n- 当前仓库已有 GitHub Pages 前端部署流程，直接切到 Vercel 会改变现有发布方式\n- 现在就删除 `apps/node_backend/vercel.json` 会失去回退路径\n\n## 建议把 issue 改成下面这个范围\n\n### 新目标\n验证 `askman-dev/bricks` 是否适合在 Vercel 上实现同域名部署：\n- `https://bricks.askman.dev/` → Flutter Web 构建产物\n- `https://bricks.askman.dev/api/*` → Node backend\n\n### 拆分任务\n- [ ] 确认 `apps/node_backend` 是否需要改造成 Vercel 兼容的 handler / serverless 入口，而不是直接 `app.listen(...)`\n- [ ] 确认 Flutter Web 能否在目标部署流程中稳定产出静态文件\n- [ ] 确认根级部署配置是否真的适合当前 monorepo\n- [ ] 保留现有 `apps/node_backend/vercel.json` 直到新方案验证通过\n- [ ] 评估是否继续保留 GitHub Pages 前端部署，还是完全切换到 Vercel\n- [ ] 设计生产 / preview / 本地开发三套 GitHub OAuth callback 策略\n- [ ] 明确敏感环境变量仅服务端可见，避免前端构建误注入\n- [ ] 制定回滚方案\n\n### 建议验收标准\n- [ ] `https://bricks.askman.dev/` 可以正常加载 Flutter Web 首页\n- [ ] `https://bricks.askman.dev/api/health` 返回 200\n- [ ] GitHub OAuth 回调 `https://bricks.askman.dev/api/auth/github/callback` 可正常工作\n- [ ] Preview 环境不会误用生产 OAuth 配置\n- [ ] 统一部署失败时可以回退到当前前后端各自部署方式\n\n### 建议标题\n`feat: 验证并落地 Vercel 同域名前后端统一部署`\n\n如果你同意，我下一步建议直接把 issue body 按上面的结构重写，避免执行人误以为当前方案已经验证可上线。