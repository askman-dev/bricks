name: platform_bridge
description: Cross-platform bridge for Bricks. Handles filesystem permissions, sandbox paths, local HTTP server, WebView/browser integration, and device differences.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
