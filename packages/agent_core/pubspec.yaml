name: agent_core
description: Intelligence runtime for the Bricks agent system. Implements the agent run loop, context management, tool execution, sub-agent delegation, skills, permissions, and provider abstraction.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  agent_sdk_contract:
    path: ../agent_sdk_contract
  meta: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
