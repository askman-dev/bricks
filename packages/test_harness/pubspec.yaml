name: test_harness
description: Test support layer for Bricks. Provides fake providers, fake filesystem, sample workspaces/projects, and e2e fixture helpers.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  agent_sdk_contract:
    path: ../agent_sdk_contract
  workspace_fs:
    path: ../workspace_fs
  project_system:
    path: ../project_system
  chat_domain:
    path: ../chat_domain
  meta: ^1.9.0
  test: ^1.24.0

dev_dependencies:
  lints: ^3.0.0
