# AGENTS

## Environment bootstrap
- Before running Flutter/Dart workspace checks in this repository, run `./tools/init_dev_env.sh` from the repo root.
- `tools/init_dev_env.sh` is the standard bootstrap entrypoint for both local and Codex/container environments.

## Plan persistence
- For each feature or task, create and save a markdown plan file in `docs/plans/`.
- Name plan files with a `YYYY-mm-dd-hh-mm` prefix.

## GitHub Agentic Workflows (gh-aw)

This repository uses [GitHub Agentic Workflows](https://github.github.com/gh-aw/) to run AI coding agents on a schedule or in response to repository events.

### Workflow files

Agentic workflow source files live in `.github/workflows/` alongside standard GitHub Actions workflows:

| File | Purpose |
|---|---|
| `.github/workflows/pr-review.md` | Reviews PRs using the `bricks-code-reviewer` agent |
| `.github/workflows/pr-review.lock.yml` | Compiled GitHub Actions YAML (generated by `gh aw compile`) |

### Custom agents

Copilot custom agent files live in `.github/agents/`:

| File | Purpose |
|---|---|
| `.github/agents/bricks-code-reviewer.agent.md` | Domain-specific review criteria for Dart/Flutter and Node.js code |

### Setup

1. Install the `gh-aw` CLI extension:
   ```sh
   gh extension install githubnext/gh-aw
   ```

2. Add the required GitHub Actions secret (`COPILOT_GITHUB_TOKEN`) — a fine-grained PAT with **Copilot Requests: Read** permission — to the repository:
   ```sh
   gh aw secrets set COPILOT_GITHUB_TOKEN --value "<your-copilot-pat>"
   ```

3. Compile any uncompiled workflow to generate its `.lock.yml`:
   ```sh
   gh aw compile pr-review
   ```
   Commit both the `.md` and the generated `.lock.yml`.

4. Verify the setup:
   ```sh
   gh aw status
   ```

### Adding a new agentic workflow

1. Create `.github/workflows/<name>.md` with YAML frontmatter and a natural-language prompt body.
2. Run `gh aw compile <name>` to produce `.github/workflows/<name>.lock.yml`.
3. Commit both files.
